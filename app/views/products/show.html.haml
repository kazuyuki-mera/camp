%h1 show画面
= link_to products_path do
  一覧へ
.content__row
  = @product.name
  = @product.price
  = @product.content
  = @product.product_images.first.path
  -# = image_tag "#{@product.product_images.first.path}"
= link_to new_product_reservation_path(@product) do
  予約する